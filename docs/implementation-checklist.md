# Чеклист закрытия плана

## Сделано

- [x] `module2`: расширен ЛК заказов (таймлайн, группировка, фильтры, статистика).
- [x] `module2`: достижения/награды/история активности в локальной модели.
- [x] `module2` + `astrakhan-admin`: этап 2 синхронизации прогресса (snapshot get/sync по email).
- [x] `module3`: устранен рассинхрон `/api/metrics/activity` и добавлен fallback в UI.
- [x] `module3`: добавлены эксплуатационные метрики (`/api/metrics/ops`).
- [x] `module3`: добавлены API и виджеты business funnel и качества данных.
- [x] `astrakhan-admin`: точечные UX-улучшения форм `poi/routes/products` + быстрые действия `orders/view`.
- [x] `astrakhan-admin`: унифицированы действия "черновик / опубликовать / отмена" в формах `poi/routes/products`.

## Следующие пункты (к закрытию)

- [x] `module2`: серверный учет подтвержденных событий прохождения маршрута (не косвенные расчеты).
- [x] `module2`: выбор конкретного платного маршрута при использовании награды (бизнес-flow end-to-end).
- [x] `module2`: строгий фильтр заказов по типу маршрута из серверного контракта.
- [x] `astrakhan-admin`: sticky summary справа с прогрессом обязательных полей и переходами к ошибкам.
- [x] `astrakhan-admin`: предпросмотр карточек "как увидит пользователь" для `poi/routes/products`.
- [x] `module3`: реальный расчет latency p50/p95/p99 из таймингов запросов.
- [x] `module3`: алертинг (пороговые правила + канал уведомлений + история срабатываний).
- [x] `infra`: SQL-миграция `user_progress_profiles` для всех окружений.
